============
Installation
============

At the command line::

    $ pip install networking-sfc

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-sfc
    $ pip install networking-sfc
